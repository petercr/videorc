# 2026-06-22 Videorc 0.9.0 Beta 1 Acceptance

This is the working acceptance record for Beta 1. It is not a passing release
candidate yet; it records the exact evidence gathered while wiring the signed
DMG and authenticated download path.

## Artifact

- Desktop commit: `32fd86d2` (`Document beta download rollback`)
- Web commit: `6924b931` (`Add public macOS download entrypoint`)
- Workflow run: BLOCKED - hosted GitHub Actions budget prevents jobs from starting
- DMG filename: `Videorc-0.9.0-mac-arm64.dmg` (local unsigned dist proof)
- DMG SHA256: `754b41aa64a0ff2aa4a3fe2398bb72d17b65d89d9d4b3fa09ac1822f8d3d40b6`
- App version: `0.9.0`
- Architecture: `arm64`
- macOS version: local build host only; clean-machine pass not run
- Machine/user cleanliness: BLOCKED - no signed/notarized DMG available yet

## Build-Machine Gates

- `pnpm test:scripts`: PASS - 293 script tests passed
- `pnpm typecheck`: PASS
- `pnpm lint`: PASS
- `pnpm format:check`: PASS on desktop release slice
- `pnpm package:preflight:macos`: PASS
- `pnpm dist:desktop`: PASS - unsigned local DMG proof only
- `pnpm release:manifest:macos`: PASS - wrote `release.json` and SHA256 sidecar
- `pnpm smoke:packaged:bundled`: PASS - packaged app used bundled FFmpeg and FFprobe; Screen + camera, Screen only, Camera only, and Side-by-side recordings passed analyzer checks
- `pnpm smoke:packaged:native-preview`: PASS - packaged app reported native-surface/cametal-layer preview with no fallback image polling or blank frames
- `pnpm release:preflight:macos`: BLOCKED - local and Videorc GitHub repo are missing `CSC_LINK`, `CSC_KEY_PASSWORD`, `APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, and `APPLE_TEAM_ID`
- `pnpm release:secrets:macos`: BLOCKED - Videorc GitHub repo is missing required signing/notarization and private download storage secrets
- `pnpm release:upload:preflight:macos`: BLOCKED without private storage env values; PASS with dummy S3-compatible env fixture
- Beta release manifest default URL: PASS - release notes now point to `https://videorc.app/releases/0.9.0-beta.1`
- `pnpm dist:desktop:signed`: BLOCKED - signing/notarization secrets are missing
- `pnpm release:validate:macos`: BLOCKED - signed artifact is not available
- `pnpm release:upload:macos`: BLOCKED - signed artifact and private storage secrets are not available
- Artifact upload/download path: BLOCKED - release workflow cannot start while Actions budget, signing secrets, and private storage secrets are missing
- Release metadata path: `apps/desktop/release/release.json` from local unsigned proof
- SHA256 sidecar path: `apps/desktop/release/Videorc-0.9.0-mac-arm64.dmg.sha256`

## Authenticated Web Download Gate

- Web storage config tests: PASS - focused download tests passed, including manifest precedence over static release metadata
- Web authenticated route tests: PASS - unauthorized, unavailable, misconfigured, manifest-backed, and signed-in redirect cases passed
- Web account download metadata: PASS - signed-in account download card resolves private `release.json` metadata when configured
- Web release notes page: PASS - `/releases/0.9.0-beta.1` exists and is listed in the sitemap
- Public macOS download entrypoint: PASS - `/download/mac` sends signed-out users through login and signed-in users to the private macOS download API
- Web full tests: PASS - 171 tests passed under Node 24
- Web typecheck: PASS
- Web lint: PASS
- Web touched-file formatting: PASS
- Private storage object key: expected `releases/macos/0.9.0-beta.1/Videorc-0.9.0-mac-arm64.dmg`
- `release.json` uploaded to private storage: BLOCKED - signed artifact not available
- DMG uploaded to private storage: BLOCKED - signed artifact not available
- Web download environment configured: BLOCKED - production storage object and env values not installed yet; `.env.example` now documents manifest-backed Beta 1 download configuration
- Anonymous request to `/api/downloads/macos/latest` is rejected: PASS in route test
- Signed-in request redirects to a private presigned DMG URL: PASS in route test
- Downloaded DMG SHA256 matches `release.json`: BLOCKED - production storage download not available yet

## Clean-Machine Gatekeeper

- `spctl --assess --type open --context context:primary-signature --verbose <dmg>`:
  BLOCKED - signed/notarized DMG not available
- `xcrun stapler validate <dmg>`: BLOCKED - signed/notarized DMG not available
- `hdiutil attach <dmg>`: BLOCKED - signed/notarized DMG not available
- `spctl --assess --type execute --verbose <Videorc.app>`: BLOCKED - signed/notarized app not available
- `xcrun stapler validate <Videorc.app>`: BLOCKED - signed/notarized app not available
- First launch accepted without override: BLOCKED - signed/notarized app not available

## Permissions

- Camera permission prompt and grant: BLOCKED - clean-machine pass not run
- Microphone permission prompt and grant: BLOCKED - clean-machine pass not run
- Screen Recording permission prompt and grant: BLOCKED - clean-machine pass not run
- Relaunch after permission grant: BLOCKED - clean-machine pass not run

## Packaged Smokes

- `VIDEORC_PACKAGED_APP_EXECUTABLE=... pnpm smoke:packaged:bundled`: PASS on local unsigned packaged app
- `VIDEORC_PACKAGED_APP_EXECUTABLE=... pnpm smoke:packaged:native-preview`: PASS on local unsigned packaged app
- Packaged backend reports `READY`: PASS
- Bundled FFmpeg used: PASS
- Bundled FFprobe used by analyzers: PASS
- Native preview reports CAMetalLayer: PASS
- No production JPEG-polling fallback: PASS

## Manual Real-Source Recording

- Screen source: BLOCKED - clean-machine pass not run
- Camera source: BLOCKED - clean-machine pass not run
- Microphone source: BLOCKED - clean-machine pass not run
- Recording duration: BLOCKED - clean-machine pass not run
- Recording path: none committed
- Analyzer/support bundle path, if generated: none committed
- Playback smoothness: BLOCKED - clean-machine pass not run
- Audio/video sync by eye: BLOCKED - clean-machine pass not run
- Preview currentness by eye: BLOCKED - clean-machine pass not run

## Failures And Follow-Up

- Signing secrets are not installed in `TheOrcDev/videogre`; AgentPacks has the matching secret names, but GitHub does not expose values for copying.
- Private download storage secrets are not installed in `TheOrcDev/videogre`; `pnpm release:secrets:macos` lists the missing required names without exposing secret values.
- Hosted release workflow cannot start because the GitHub Actions budget is exhausted.
- Production private storage upload and web environment configuration are waiting on a signed DMG and storage secrets.
- Rollback path is documented in `docs/distribution.md`; the web route can fail closed by disabling download storage or point back to the previous accepted manifest.

## Verdict

- Overall release-candidate verdict: BLOCKED
- Operator: Codex
- Notes: The release/download implementation, private upload tooling, packaged native preview proof, and manifest-backed web download metadata are in place, but Beta 1 is not shippable until signing secrets, storage secrets, Actions budget, signed/notarized artifact validation, private storage upload, web env configuration, clean-machine Gatekeeper, permissions, and manual real-source recording all pass.
